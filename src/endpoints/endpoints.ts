const SERVER = 'https://localhost:5000/';

export const PRODUCT = `${SERVER}product`;

export const PRODUCT_ID = (id:string)=>`${SERVER}product/${id}`;

export const CATEGORY = `${SERVER}category`;

export const CATEGORY_ID = (id:string)=>`${SERVER}category/${id}`;
